import { defineStore } from 'pinia'

export const UseMain = defineStore('main', {
    state: () => ({}),
    actions: {
        async fetch() {
            // Fetch data from the server
        },
    },
})
